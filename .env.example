# Example environment variables for deployment / local migrations
# Replace the DATABASE_URL with your Neon (Postgres) connection string
DATABASE_URL=postgresql://username:password@db.neon.com:5432/dbname

# When deploying to Vercel, set this same variable in the project environment settings
DATABASE_URL=postgresql://user:password@ep-instance-name.neon.tech:5432/neondb
